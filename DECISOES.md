# Registro de Decisões

## Decisões Iniciais Confirmadas

**Data:** 06/08/2026
**Decisão:** 1. O projeto será um site institucional separado da Plataforma Operacional Almeida.
**Motivo:** Garantir que o foco e a estrutura técnica sejam voltados para apresentação institucional sem misturar com sistema operacional.
**Impacto:** Sistemas, bancos de dados e infraestrutura separados.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 2. Stack prevista: Next.js + TypeScript.
**Motivo:** Escolha técnica para garantir robustez, SEO e escalabilidade futura.
**Impacto:** Define as ferramentas para desenvolvimento.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 3. Hospedagem prevista: Vercel, em projeto próprio.
**Motivo:** Integração nativa com Next.js e separação de ambiente da plataforma.
**Impacto:** Arquitetura de deploy focada na Vercel.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 4. A pasta ainda NÃO possui conexão com repositório remoto.
**Motivo:** Início do projeto focado exclusivamente na fundação documental.
**Impacto:** Código será versionado apenas localmente nesta etapa inicial.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 5. O projeto ainda NÃO existe na Vercel.
**Motivo:** Infraestrutura será criada em momento posterior.
**Impacto:** Sem ambiente de preview ou produção até o momento.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 6. Nenhuma dessas conexões será criada nesta tarefa.
**Motivo:** Manter o escopo focado.
**Impacto:** Nenhuma configuração de infra externa nesta fase.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 7. O site prioriza posicionamento institucional e reputação, não uma abordagem agressiva de conversão.
**Motivo:** O objetivo é servir como cartão de visitas e transmitir credibilidade.
**Impacto:** Tom de voz e estrutura do site adaptados para institucionais.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 8. As empresas do Grupo Almeida devem aparecer como partes de um ecossistema único, preservando a identidade própria da Saturno quando necessário.
**Motivo:** Consolidar a marca enquanto respeita a força regional da Saturno.
**Impacto:** Design e arquitetura da informação refletirão esta visão unificada.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 9. Conteúdo factual não validado deve permanecer identificado como pendente.
**Motivo:** Evitar publicações com informações falsas ou não confirmadas.
**Impacto:** Revisão rigorosa de informações antes de ir ao ar.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 10. Pagamento confirmado em 50% no início e 50% na entrega.
**Motivo:** Acordo comercial firmado.
**Impacto:** Gestão financeira do projeto.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 11. Versão em inglês incluída como brinde/cortesia.
**Motivo:** Decisão comercial de não cobrar como adicional.
**Impacto:** O escopo inclui a internacionalização como cortesia, não como item faturado extra.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 12. O site antigo comprometido deverá ser tratado no planejamento de go-live, sem realizar intervenção nesta etapa.
**Motivo:** Proteger SEO e reputação no momento de substituição, mas focar na base atual.
**Impacto:** Atividade adicionada ao checklist de go-live futuro.
**Status:** Confirmado.

**Data:** 06/08/2026
**Decisão:** 13. Adotar diário de trabalho separado por data, usando `diario/AAAA-MM-DD.md`, e manter `diario/HISTORICO.md` apenas como índice resumido.
**Motivo:** Evitar que um único arquivo de histórico cresça indefinidamente e preservar contexto suficiente para retomadas, troca de agentes e investigação futura sem sobrecarregar cada nova sessão.
**Impacto:** O fechamento de cada jornada passa a seguir `FECHAR_DIA.md`. O diário diário guarda a história detalhada. `HISTORICO.md` guarda apenas data e resumo curto. Decisões estruturais continuam em `DECISOES.md`. Pendências continuam em `DEPOIS.md`. Acessos continuam em `ACESSOS.md`.
**Status:** Adotado.

**Data:** 06/08/2026
**Decisão:** 14. Fundação técnica criada na raiz do repositório com Next.js 16.3.0 (App Router), React 19.2.8 e TypeScript 7.0.2, sem diretório `src/`, sem Tailwind e sem ESLint configurados nesta etapa.
**Motivo:** Não havia versão fixada nos documentos vivos; foram usadas as versões estáveis mais recentes disponíveis no momento da instalação. Tailwind e ESLint não foram adicionados por não estarem explicitamente definidos como parte da stack e para manter a instalação restrita ao mínimo necessário para o primeiro build/deploy válido.
**Impacto:** `package.json`, `tsconfig.json` (gerado automaticamente pelo Next.js) e a pasta `app/` passam a existir na raiz. Deploy da Vercel poderá ser tentado novamente. Ferramentas de estilo (Tailwind) e lint (ESLint) ficam em aberto para decisão futura quando o design/conteúdo entrar em pauta.
**Status:** Confirmado.

**Data:** 07/08/2026
**Decisão:** 15. Durante a fase de desenvolvimento, prototipação e validação com a Almeida, o projeto continua publicado na conta Vercel da Sartec/Lucas, funcionando como ambiente de desenvolvimento, preview e homologação, aproveitando o workflow já configurado com GitHub. A provável infraestrutura definitiva de produção do site será criada posteriormente em uma conta Cloudflare pertencente ao próprio Grupo Almeida, com a Almeida mantendo o controle principal da conta e a Sartec Digital recebendo acesso administrativo para manutenção. A decisão final sobre a hospedagem de produção será confirmada antes do go-live; até lá, Cloudflare não será configurado e o deploy atual na Vercel não será alterado.
**Motivo:** Evitar que domínio, hospedagem e operação do site fiquem dependentes da conta pessoal de um prestador, mantendo ao mesmo tempo o workflow de desenvolvimento já funcional com GitHub + Vercel enquanto o site está em construção e validação com o cliente.
**Impacto:**
1. Desenvolvimento e previews continuam normalmente via GitHub + Vercel.
2. O código deve se manter tão portátil quanto razoavelmente possível.
3. Quando existir solução web padrão e solução proprietária da Vercel para a mesma necessidade, preferir a solução portável, desde que isso não prejudique qualidade, desempenho ou complexidade do projeto.
4. Não adicionar dependência de serviços proprietários da Vercel sem necessidade real; caso alguma funcionalidade futura justifique isso, registrar e avaliar a decisão antes da implementação.
5. Recursos visuais planejados — animações controladas por scroll, vídeo de background, Canvas/WebGL, experiências 3D, sequências de frames e contadores institucionais calculados no navegador — são compatíveis com esta estratégia e não devem ser simplificados por causa da possível migração de hospedagem.
6. O site continua tratado preferencialmente como aplicação institucional de forte componente estático/client-side. Necessidade futura de backend, função server-side ou outro processamento não está proibida, mas deverá ser avaliada também pela portabilidade para a infraestrutura definitiva.
7. Próximo ao go-live, haverá etapa específica de validação de compatibilidade com Cloudflare antes da migração.
8. A infraestrutura definitiva deve ficar sob controle do Grupo Almeida, evitando que domínio, hospedagem e operação do site fiquem dependentes da conta pessoal de um prestador.
9. A troca do domínio oficial só ocorrerá depois que a versão de produção estiver publicada e testada na infraestrutura definitiva.
10. Na futura alteração de DNS, os registros relacionados ao serviço de e-mail da Almeida devem ser preservados; a migração do site não implica automaticamente migração dos e-mails.
**Status:** Confirmado quanto ao papel atual da Vercel (desenvolvimento/preview/homologação). Estratégia de produção na Cloudflare é provável, mas depende de confirmação final antes do go-live.

**Data:** 12/08/2026
**Decisão:** 16. Nesta fase inicial de implementação do site institucional (primeira versão funcional da Home), a Vercel é usada como ambiente de prototipação e homologação visual com a Helóra/cliente. Como ainda não existe uma versão anterior do site em produção nesse projeto Vercel (`almeidaambiental`) que precise ser preservada, fica autorizado publicar diretamente pela branch `main` durante esta etapa inicial, sem exigir passagem por ambiente de preview antes de cada publicação.
**Motivo:** O endereço do projeto Vercel ainda não é usado pelo cliente final; usá-lo como homologação evita esforço extra de gestão de branches de preview enquanto o site está em construção e validação de direção visual.
**Impacto:** Commits com mudanças funcionais da Home podem ir direto para `main` nesta etapa, com deploy automático da Vercel. A Regra 7 de `AGENT_RULES_SITE.md` (nenhuma publicação em produção sem validação humana explícita / sempre passar por preview) fica temporariamente flexibilizada apenas para este cenário específico (Vercel = prototipação, sem produção real do cliente ainda no ar). Assim que existir uma versão em produção real usada pelo cliente, essa flexibilização deixa de valer e a Regra 7 volta a ser seguida integralmente.
**Status:** Confirmado para esta fase inicial.

**Data:** 12/08/2026
**Decisão:** 17. Nova arquitetura de informação do site, solicitada pela Helóra: Home / Grupo Almeida, Almeida Ambiental, Almeida Equipamentos, Saturno Ambiental, Contato. Não haverá página independente de Sustentabilidade.
**Motivo:** Decisão da cliente. Conteúdos de sustentabilidade continuam podendo aparecer dentro da Home, da Almeida Ambiental, da página institucional do Grupo ou de outras partes relevantes do site — apenas deixam de ter uma página própria no menu.
**Impacto:** Esta arquitetura substitui a arquitetura inicial prevista em `CONTEXTO_PROJETO_SITE.md` (que incluía "Sustentabilidade" como página independente). O menu principal do site (item de navegação do header) passa a conter apenas: Almeida Ambiental, Almeida Equipamentos, Saturno Ambiental, Contato — com o logo do Grupo Almeida cumprindo o papel de link para a Home, sem precisar de um item "Home" redundante no menu.
**Status:** Confirmado.

**Data:** 12/08/2026
**Decisão:** 18. Direção visual da Home (primeira versão funcional): paleta background `#F7F6F1`, foreground `#15150F`, primary `#4E6B24`, secondary `#C7D79E`, muted `#ECEBE2`, card `#FBFAF7`, tipografia de destaque Playfair Display (via `next/font/google`). Um tom dourado/amarelo (`#D8A536`, provisório) foi centralizado como token para o destaque de "RESULTADO" na primeira dobra, por ainda não existir um valor oficial confirmado.
**Motivo:** Direção visual fornecida diretamente para a implementação desta Home, distinta das duas direções ("A — Institucional Técnica" e "B — Ambiental Aberta") registradas em `design/tokens.css` e `design/GUIA_MONTAGEM_FIGMA.md` (que usam Inter Tight/Plus Jakarta Sans e paletas verdes diferentes).
**Impacto:** Os tokens desta direção foram centralizados em `app/globals.css` (não reaproveitam as variáveis de `design/tokens.css`). Quando a direção visual definitiva do site for consolidada com o Figma/cliente, avaliar se essa nova paleta substitui as direções A/B do design system ou se passa a ser registrada formalmente como "Direção C" nos documentos de design.
**Status:** Confirmado para a Home desta etapa. Consolidação com o design system do Figma fica pendente.

**Data:** 12/08/2026
**Decisão:** 19. O vídeo `Video_Almeida_15_seg.mp4`, que já existia em `videos/` na raiz do repositório, foi movido (não duplicado nem re-codificado) para `public/videos/Video_Almeida_15_seg.mp4`.
**Motivo:** Arquivos servidos como asset estático pelo Next.js (com suporte nativo a range requests, necessário para o seek suave do vídeo controlado por scroll) precisam estar dentro de `public/`. O arquivo continua sendo exatamente o mesmo (mesmo conteúdo, mesmo nome), apenas em outro caminho.
**Impacto:** Qualquer referência futura ao vídeo original deve usar o novo caminho `public/videos/Video_Almeida_15_seg.mp4` (servido em produção como `/videos/Video_Almeida_15_seg.mp4`).
**Status:** Confirmado.

**Data:** 13/08/2026
**Decisão:** 20. Segunda rodada de ajustes na experiência de scroll da Home: substituída a rolagem contínua (uma faixa única e muito alta, ~804svh, onde cada pixel de scroll movia o vídeo) por dez dobras de 100svh cada, com CSS Scroll Snap (`scroll-snap-type: y mandatory`, `scroll-snap-align: start`, `scroll-snap-stop: always`, `scroll-padding-top` considerando o header fixo). O vídeo e o overlay passaram a ficar em uma camada `position: fixed` atrás das dobras, e o tempo do vídeo é interpolado continuamente entre os tempos de duas dobras adjacentes conforme a posição de scroll, com correção explícita de `currentTime` ao final de cada parada. Uma trava adicional em JS (`wheel`, apenas desktop/mouse/trackpad) garante que uma rolagem forte não avance mais de uma dobra por vez — em teste automatizado, o CSS Scroll Snap sozinho deixou passar até 3 dobras em uma rolagem de wheel sintética muito grande; com a trava, ficou limitado a 1. No touch/mobile não há interceptação, apenas o Scroll Snap nativo. Também foi identificado e corrigido o motivo do vídeo aparecer quase invisível (só o verde do overlay) no mobile: em alguns navegadores mobile o decoder de vídeo só desenha frames depois de um `play()` real; foi adicionado um `play()` seguido de `pause()` imediato (mudo, inline) logo após `loadeddata`, sem autoplay perceptível ao usuário. Os tempos de `SCROLL_STOPS` (`lib/scroll-timeline.ts`) não foram alterados. Foram adicionados marcadores temporários "DOBRA 0X · Y.YYs" nas dobras 2 a 10 (ainda sem conteúdo), centralizados em `components/DevSectionMarker.tsx` e no bloco `.dev-section-marker` de `app/globals.css`, para facilitar a remoção quando o conteúdo definitivo dessas dobras for implementado.
**Motivo:** A cliente relatou que o comportamento anterior era excessivamente contínuo (difícil perceber onde uma dobra terminava e a próxima começava) e que no mobile a tela ficava praticamente tomada pelo verde do overlay, sem o vídeo perceptível.
**Impacto:** `components/ScrollVideoExperience.tsx` e `lib/scroll-timeline.ts` foram reescritos; `app/globals.css` ganhou as classes `.video-layer`, `.snap-container`, `.snap-section` e perdeu `.scroll-track`, `.scroll-stage`, `.scroll-anchor` (não usadas mais). Nenhum ajuste de paleta, tipografia, header, menu, textos da dobra 1 ou crop do vídeo (`object-fit: cover`) foi feito. O vídeo continua sendo exatamente o arquivo já existente em `public/videos/Video_Almeida_15_seg.mp4`, sem reencode.
**Status:** Confirmado, aguardando validação visual do cliente no deploy.

**Data:** 13/08/2026
**Decisão:** 21. Terceira rodada de ajustes na Home, em duas fases. Fase 1 (conteúdo): a Home passou de dez para nove dobras narrativas; o antigo stop `7.04s` deixou de ser parada (o vídeo continua atravessando esse instante durante a reprodução, apenas não pausa mais ali) e a timeline final de `SCROLL_STOPS` (`lib/scroll-timeline.ts`) ficou em `0.00 / 2.13 / 4.03 / 5.09 / 6.13 / 7.28 / 10.21 / 12.10 / 15.00`. As dobras 2 a 9 receberam o conteúdo institucional definitivo fornecido pela cliente (componentes novos em `components/HomeSections.tsx`), os marcadores temporários "DOBRA 0X" foram removidos (`components/DevSectionMarker.tsx` apagado) e um footer institucional real foi criado (`components/Footer.tsx`), exibido após a dobra 9 fora da camada fixa do vídeo. A dobra 1 (hero), o header e o menu não foram alterados. Fase 2 (motor de vídeo): o modelo antigo, em que o `currentTime` era interpolado a cada pixel de scroll, foi substituído por transições discretas por dobra — na descida, o próprio `<video>` toca (`play()`) do tempo de origem até o tempo alvo (acelerando moderadamente, até no máximo 2x, só nos trechos com mais de 1,8s reais, para não ficar lento) e é pausado e corrigido exatamente no tempo da dobra ao chegar; na subida, sem reprodução reversa nativa confiável, o `currentTime` é interpolado via `requestAnimationFrame` com easing. Uma única trava (`isTransitioningRef`) evita empilhar navegações, compartilhada entre o gesto de wheel (desktop) e a detecção de cruzamento de metade do caminho via scroll (usada também no touch/mobile, que continua sem interceptação). Também foi corrigida a inicialização do decoder de vídeo no mobile: além de aguardar `loadeddata`, agora também é iniciada a partir da primeira interação real do usuário (touch/scroll/tecla/wheel/pointer), e o mesmo mecanismo passou a valer também no modo `prefers-reduced-motion` (antes só existia no modo normal, e o `<video>` desse modo nem tinha `ref` — por isso podia nunca desenhar nenhum frame). Erros de `video.play()` deixaram de ser silenciados (agora vão para `console.warn`).
**Motivo:** Pedido direto da cliente após validar a estrutura anterior: manter a navegação de uma dobra por vez (já aprovada), mas trocar a sensação de "slides" por movimento contínuo real do vídeo na descida, e investigar/corrigir a tela verde chapada no mobile em vez de apenas ajustar opacidade.
**Impacto:** `lib/scroll-timeline.ts`, `components/ScrollVideoExperience.tsx`, `app/page.tsx`, `app/globals.css` foram alterados; `components/HomeSections.tsx` e `components/Footer.tsx` foram criados; `components/DevSectionMarker.tsx` foi removido. O arquivo de vídeo `Video_Almeida_15_seg.mp4` não foi trocado nem reencodado. O gap de acessibilidade em `prefers-reduced-motion` (dobras 2-9 sem conteúdo real nesse modo) foi registrado em `DEPOIS.md`, não corrigido nesta rodada.
**Status:** Confirmado, aguardando validação visual do cliente no deploy (inclusive em dispositivo mobile físico, per pendência já registrada sobre o GOP de 5s do vídeo).

**Data:** 13/08/2026
**Decisão:** 22. Rodada de arquitetura visual do conteúdo (dobras 2 a 9) e correção do acesso ao footer, sem tocar no motor de vídeo nem na dobra 1. As dobras 2 a 9 deixaram de reutilizar a estrutura do Hero (`.hero-content-layer`/`.hero-content`/`.hero-title`, ancorada no canto inferior esquerdo) e passaram a usar um sistema próprio: `.section-layer` (camada de posicionamento, substitui `.hero-content-layer` só nessas dobras) + `.section-grid` (área útil centralizada, até 1320px, grid conceitual de 12 colunas a partir de 1024px; empilha em coluna única no mobile). Cada dobra (`.dobra-02` a `.dobra-09`) tem sua própria distribuição de título/corpo/CTA no grid, definida em `app/globals.css`, criando composições variadas (não uma sequência mecânica de banners nem uma alternância esquerda/direita mecânica). Novos tokens tipográficos: `.section-title` (headline institucional das dobras 2-7, claramente menor que `.hero-title`), `.closing-title` (encerramento da dobra 9, aproxima-se do Hero sem repetir o mesmo componente) e `.stat-value` aumentado (indicadores da dobra 8, maior escala depois do Hero). `.stats-grid` passou a ser 1 coluna no mobile e 2×2 a partir de 1024px (antes era 2×2 no mobile e 1×4 a partir de 640px — invertido). Nenhuma palavra da copy das dobras 2 a 9 foi alterada; só as classes CSS/wrapper mudaram. O footer ganhou `scroll-snap-align: start` (sem `scroll-snap-stop: always`), tornando-se um destino válido do mesmo `scroll-snap-type: y mandatory` do documento, sem entrar em `SCROLL_STOPS` nem em `sectionRefs` — o motor de vídeo continua reagindo só às 9 dobras. A única linha tocada em `components/ScrollVideoExperience.tsx` foi o `className` do wrapper de conteúdo das dobras 2-9 (`hero-content-layer` → `section-layer`); nenhuma função do motor (`playForward`, `interpolateBackward`, `transitionTo`, o handler de `wheel`, o efeito de scroll/opacidade) foi alterada.
**Motivo:** A cliente avaliou que as dobras 2 a 9 pareciam cópias do Hero (mesmo rótulo pequeno + título grande empilhado + texto preso no canto inferior esquerdo), fazendo a Home parecer uma sequência de banners em vez de conteúdo institucional, e reportou um bug em que a página voltava sozinha para a dobra 9 ao tentar rolar até o footer — causado pelo `scroll-snap-type: y mandatory` não ter nenhum ponto de encaixe válido depois da última dobra.
**Impacto:** `app/globals.css`, `components/HomeSections.tsx` e `components/ScrollVideoExperience.tsx` (uma linha) foram alterados. `lib/scroll-timeline.ts`, `components/HeroContent.tsx`, `components/Header.tsx` e o arquivo de vídeo não foram tocados. O comportamento de descida do vídeo (reprodução sequencial real) e a subida (interpolação via `requestAnimationFrame`) permanecem exatamente como na decisão 21 — inclusive o travamento conhecido da subida, que continua pendente de tratamento em rodada própria. O bug do vídeo mobile (overlay verde sem vídeo visível) também não foi tocado nesta rodada.
**Status:** Confirmado, aguardando validação visual do cliente no deploy.

**Data:** 14/08/2026
**Decisão:** 23. Quinta rodada de ajustes mobile: substituído o mecanismo de composição das dobras 2 a 9 (fluxo `flex-direction: column` com `padding-top`/`margin-top`/`gap`/`margin-top: auto` como principal responsável pela posição vertical) por uma prancheta de coordenadas própria por dobra. `.mf-section-frame`/`.mf-hero-frame` passaram a ser apenas `position: relative` (sistema de coordenadas); cada bloco gráfico (rótulo, subtítulo/localização, headline, corpo, CTA, indicadores da dobra 9) passou a ser `position: absolute` com `top` (e, quando o centro horizontal da composição não é o padrão — caso da dobra 5, centro em x=204,5px — `left`) definido por instância em `HomeSections.tsx`/`HeroContent.tsx`, medido a partir de screenshots de referência do Figma fornecidos diretamente pela cliente nesta rodada (não havia link de arquivo Figma disponível para uso do MCP; a hierarquia de fontes de verdade do item 8 desta tarefa — screenshot > código exportado > código atual — ficou reduzida a screenshot, já que código exportado do Figma não estava disponível). Os valores de X/largura/tipografia (font-size, line-height, letter-spacing) já calculados em rodadas anteriores foram preservados quase integralmente (a causa raiz relatada pela cliente era a coordenada Y, não X/largura/fonte); a exceção foi o subtítulo da dobra 2 (`Diagnóstico · Coleta · Triagem · Trituração · Descaracterização`), reduzido de 13px para ~10,5px porque em uma linha só (sem `flex-wrap`) ele não cabia nos 393px de largura. `.mf-d-subtitle` deixou de usar `flex-wrap: wrap` (causa da quebra incorreta de "Descaracterização"/"Containers" relatada) e passou a `inline-flex` com `flex-wrap: nowrap`. `.mf-hero-label` e `.mf-hero-title` (Hero) ganharam `text-align: center`/`align-items: center` — o container já estava centralizado, mas as linhas de texto dentro dele não. Acima de 393px de largura (394–1023px), `.mf-hero-frame`/`.mf-section-frame` passaram a ficar centralizados via `left: 50%; width: 393px; transform: translateX(-50%)` em vez de esticados (`inset: 0`), para a composição não ficar deslocada à esquerda em telas maiores — sem alterar nada abaixo de 394px nem no desktop (≥1024px). Também corrigidos dois bugs do motor de vídeo mobile em `ScrollVideoExperience.tsx`: (1) o crop horizontal (`applyMobileCrop`) usava sempre `videoStopIndexRef.current` (a última dobra confirmada), então durante uma transição o enquadramento continuava sendo o da dobra anterior até o fim da animação — passou a usar a dobra ALVO (`videoTargetIndexRef`) enquanto uma transição está em andamento, e reaplica o crop do alvo ao final para garantir estado determinístico; (2) o motor podia dessincronizar dobra visível × dobra do vídeo quando o usuário continuava rolando (touch/inércia) enquanto uma transição ainda tocava, porque nenhum novo destino era aceito nesse período e nada garantia uma nova checagem depois — agora, ao final de cada transição, o código confere a posição real de scroll (e um `pendingTargetIndexRef`, se `transitionTo` foi chamado de novo durante a transição) e, se ainda houver diferença, avança mais um passo (nunca um salto de várias dobras), repetindo até alcançar; testado com sequências de 8 scrolls rápidos consecutivos via Playwright (mobile emulado), sem erros de console e com convergência final sempre correta (dobra visível = dobra do vídeo = crop = `currentTime`). Os tempos de `SCROLL_STOPS` (`lib/scroll-timeline.ts`) não foram alterados nesta rodada. Trabalho feito na branch `fix/mobile-figma-coordinate-layout` (não mesclada a `main`).
**Motivo:** A cliente reportou que, apesar de builds passando e sem overflow, a Home mobile continuava visualmente diferente do Figma — com diagnóstico técnico prévio (fornecido pela própria cliente) apontando que o fluxo vertical genérico (flex-column + padding/gap/margin-top) era estruturalmente incapaz de reproduzir composições onde cada dobra tem ritmo vertical próprio (ex.: dobra 8 com grande vão entre headline e corpo; dobra 5 com centro horizontal deslocado). Pediu explicitamente que essa rodada não fosse mais uma "rodada de pequenos ajustes de margem", e sim a substituição do mecanismo.
**Impacto:** `app/globals.css`, `components/HomeSections.tsx` e `components/ScrollVideoExperience.tsx` foram alterados. `components/HeroContent.tsx` não precisou de nenhuma mudança (o fix de centralização do Hero foi só CSS, em `.mf-hero-label`/`.mf-hero-title`). `lib/mobile-fit.ts` e `lib/scroll-timeline.ts` não foram tocados. Desktop (`.desktop-only`, ≥1024px) não foi alterado (conferido via captura em 1440px antes/depois). Build (`npm run build`) e validação visual via Playwright (393px + 360/375/390/414/430px, sem overflow horizontal em nenhum) passaram. Comparação visual dobra a dobra feita por inspeção direta contra os screenshots de referência fornecidos pela cliente nesta conversa (não foram versionados no repositório, por instrução explícita da tarefa de não versionar material de comparação temporário) — não há acesso a coordenadas exatas de dev-mode do Figma (arquivo não fornecido), então os valores de `top`/`left` são medidos por proporção visual dos screenshots, não copiados de um export numérico exato; a cliente vai validar no próprio celular via Preview Deployment antes de qualquer merge.
**Status:** Confirmado quanto à arquitetura (prancheta de coordenadas por dobra) e às correções do motor de vídeo. Fidelidade pixel-a-pixel de cada dobra aguarda validação da cliente no dispositivo físico antes do merge para `main`.

**Data:** 14/08/2026
**Decisão:** 24. Sexta rodada de ajustes mobile, escopo deliberadamente restrito: uma especificação genérica (container com `scroll-snap`, header fixo com fundo translúcido, scrim/gradiente de contraste por dobra, hierarquia tipográfica e botões, em valores fixos iguais para todas as dobras) foi avaliada frente à Decisão 23 antes de qualquer implementação. Como a Decisão 23 já registra que a cliente pediu explicitamente a substituição do fluxo genérico pela prancheta de coordenadas por dobra (por incapacidade estrutural de reproduzir o ritmo vertical próprio de cada dobra do Figma), a prancheta de coordenadas (`.mf-hero-frame`/`.mf-section-frame` com filhos `position: absolute` e `top`/`left` por instância) foi mantida integralmente — nenhuma coordenada, tipografia ou estrutura por dobra foi alterada. Apenas os itens da especificação compatíveis com esse mecanismo (isto é, que não dependem de reintroduzir fluxo/`margin`/`gap` como posicionamento) foram aplicados: (1) fundo do header mobile passou de sólido (`var(--mf-bg)`) para translúcido com blur (`rgba(248,248,246,0.95)` + `backdrop-filter: blur(8px)`), para nunca deixar conteúdo vazar atrás dele durante o scroll snap; (2) todo `.mf-hero-frame`/`.mf-section-frame` ganhou um `::before` com gradiente escuro na base (`linear-gradient(180deg, rgba(0,0,0,.15) 0%, rgba(0,0,0,.4) 60%, rgba(0,0,0,.85) 100%)`, `z-index: 1`, `pointer-events: none`) para reforçar contraste de botões/indicador, com todo o conteúdo textual/CTA existente elevado para `z-index: 2` (sem mexer em nenhum `top`/`left`); (3) tokens de botão mobile atualizados para os valores informados — `.mf-btn` passou de `height: 52px`/`border-radius: 6px`/`font-size: 16px`/`font-weight: 700` para `height: 48px`/`border-radius: 8px`/`font-size: 14px`/`font-weight: 600`; `--mf-primary` (fundo do botão WhatsApp da dobra 1) mudou de `#4e6b24` para `#435e28`; o botão off-white passou a usar tokens dedicados novos `--mf-btn-offwhite-bg: #f3f3ed` / `--mf-btn-offwhite-text: #1e3a2b` (não mais `--mf-cta-light-bg`/`--mf-cta-light-text`, que continuam existindo só para o fundo do toggle de idioma ativo no header, para não acoplar os dois usos); `.mf-btn-primary` ganhou `margin-bottom: 12px` (substituindo o `gap: 24px` antes aplicado em `.mf-hero-actions`, removido). Explicitamente NÃO aplicados por dependerem do mecanismo genérico ou já estarem cobertos pela arquitetura atual: (a) o container próprio com `scroll-snap-type`/`overflow-y: scroll`/`height: 100svh` — já satisfeito pela arquitetura existente (`html` com `scroll-snap-type: y mandatory`, `.snap-section` com `height: 100svh`/`scroll-snap-align: start`), que usa página inteira + vídeo fixo atrás, não um scroller próprio; (b) hierarquia tipográfica em valores fixos (`clamp(28px,7.5vw,36px)`, cor `#E2B024` para destaque, etc.) — os valores de fonte/cor por dobra, calibrados na Decisão 23 a partir dos screenshots do Figma, foram preservados sem alteração; (c) reestruturação da seção de métricas ESG (dobra 9) em stack genérico — mantida a estrutura de lista com `top` por item já existente.
**Motivo:** Pedido do desenvolvedor trazia uma especificação genérica de refatoração mobile; ao cruzar com a Decisão 23 (pedido explícito e já confirmado da cliente contra esse mesmo mecanismo genérico), o próprio desenvolvedor confirmou, quando questionado, que o mecanismo de coordenadas por dobra deveria ser preservado e que apenas os itens compatíveis (fundo do header, overlay de contraste, botões, z-index) deveriam ser aplicados.
**Impacto:** Somente `app/globals.css` foi alterado nesta rodada (tokens em `:root`, `.site-header` no mobile, blocos `.mf-hero-frame`/`.mf-section-frame`, `.mf-hero-*`, `.mf-d-*`, `.mf-stats`, `.mf-btn*`). `components/HeroContent.tsx`, `components/HomeSections.tsx`, `components/Header.tsx`, `components/ScrollVideoExperience.tsx` e `lib/mobile-fit.ts` não foram tocados — nenhuma coordenada Y/X, nenhum texto e nenhuma estrutura JSX por dobra mudou. Build (`npm run build`) validado após a mudança.
**Status:** Confirmado quanto ao escopo aplicado (header, overlay, botões, z-index). Arquitetura de posicionamento por dobra (Decisão 23) permanece intocada e é a fonte de verdade para qualquer rodada futura de ajuste visual mobile.

**Data:** 14/08/2026
**Decisão:** 25. Sétima rodada de ajustes mobile: a pedido explícito do desenvolvedor ("esqueça essa amarra das decisões por enquanto"), a prancheta de coordenadas por dobra (Decisão 23) foi substituída por um mecanismo genérico em CSS Grid, igual para a dobra 1 (Hero) e as dobras 2 a 9: `.mf-hero-frame`/`.mf-section-frame` viraram `display: grid; grid-template-rows: auto 1fr auto;` (topo/eyebrow, centro — centralizado verticalmente pela própria track `1fr`, base — CTA + indicador de rolagem), com classes reunificadas entre Hero e dobras institucionais (`.mf-eyebrow`, `.mf-tags`, `.mf-title`/`.mf-title-gold`, `.mf-body`, `.mf-actions`, `.mf-scroll-indicator`, `.mf-metrics` para a dobra 9 em grid 2×2). Nenhuma coordenada `top`/`left` por instância sobrevive — `lib/mobile-fit.ts` (helper `mf()`) foi removido por ficar sem uso. No processo, foi identificado e corrigido um bug estrutural pré-existente (não introduzido nesta rodada, mas só exposto por ela): `scroll-padding-top` desloca cada dobra em `var(--header-height)` ao encaixar (para não ficar atrás do header fixo), mas a altura da dobra continha 100% do bloco de 100svh sem descontar esse deslocamento — os últimos ~64px de qualquer conteúdo ancorado na base (CTA, indicador) ficavam empurrados para fora da tela. Corrigido só no frame mobile (`height: calc(100svh - var(--header-height))`), sem tocar `.snap-section` nem o desktop.
**Motivo:** Pedido direto do desenvolvedor: o resultado da Decisão 24 (aplicar só os itens compatíveis com a prancheta de coordenadas) não atingiu o resultado esperado; pediu para refazer seguindo a especificação original completa, com grid, sem a restrição da Decisão 23 por enquanto.
**Impacto:** `app/globals.css`, `components/HeroContent.tsx`, `components/HomeSections.tsx` e `components/ScrollVideoExperience.tsx` (indicador de rolagem do Hero mobile passou a viver dentro de `HeroContent`, não mais duplicado em `ScrollVideoExperience`) foram alterados; `lib/mobile-fit.ts` foi removido. Desktop (`.desktop-only`, ≥1024px), o motor de vídeo (`transitionTo`/`playForward`/`interpolateBackward`) e `lib/scroll-timeline.ts` não foram tocados. Build (`npm run build`) e validação visual via Playwright (360px e 393px, todas as 9 dobras + footer) sem overflow horizontal, sem conteúdo cortado e sem erros de console.
**Status:** Confirmado. Esta decisão substitui o mecanismo da Decisão 23 (prancheta de coordenadas por dobra); a fidelidade pixel-a-pixel ao Figma deixou de ser o critério — o critério passou a ser a estrutura genérica em grid, alinhada e responsiva, descrita aqui. Fidelidade fina de espaçamento/tipografia ao Figma real (se ainda necessária) fica para validação futura da cliente.

**Data:** 14/08/2026
**Decisão:** 26. A composição mobile da Home volta a ser tratada como direção de arte por dobra. A Decisão 25 continua válida para o desktop, mas deixa de valer para a geometria mobile. Em viewport mobile, cada dobra possui coordenadas próprias e independentes. A referência mestre é 393px.
**Motivo:** O mecanismo genérico em CSS Grid da Decisão 25 (`grid-template-rows: auto 1fr auto`, centralização automática, tipografia/espaçamento únicos para todas as dobras) provou-se estruturalmente incapaz de reproduzir composições onde cada dobra tem ritmo vertical, tipografia e centro horizontal próprios (ex.: dobra 8 com grande vão entre headline e corpo; dobra 5 com centro horizontal deslocado para x=204,5px; dobra 9 com métricas em coluna única, não grid 2×2). Especificação numérica completa (posições X/Y, tipografia, cores, quebras de linha) fornecida diretamente para esta rodada, eliminando a necessidade de inferência visual a partir de screenshots.
**Impacto:** Desktop (`>=1024px`, `.desktop-only`, classes `.hero-frame`/`.section-frame`/`.dobra-*`) permanece exatamente como estava — mecanismo da Decisão 25 intocado. Mobile (`<1024px`, `.mobile-fidelity`) passa a usar um novo conjunto de classes (`.mf-hero-frame`/`.mf-section-frame` como sistema de coordenadas `position:relative`, filhos `position:absolute` com `top`/`left` por instância, `lib/mobile-fit.ts` restaurado para escalar X/largura/fonte entre 360px e 393px). `scroll-padding-top` passa a ser `0` no mobile (o header é `fixed` e fica sobre a `snap-section`, que ocupa 100% da tela sem desconto de altura de header). O scrim preto global (`::before` das dobras) deixa de existir no mobile, já que as novas classes não o herdam.
**Status:** Confirmado.

**Data:** 15/08/2026
**Decisão:** 27. (Registro retroativo — a composição já existia no código e já era chamada de "Decisão 27" em comentários de `app/globals.css` e `components/DesktopDobra.tsx`, mas nunca havia sido lançada aqui; corrigido nesta rodada, sem alterar o que já estava implementado.) Composição editorial própria para o desktop (`>=1024px`), independente da Decisão 26 (mobile). Cada dobra usa um "stage" (`.d-stage`, altura `calc(100svh - var(--header-height))`, largura fluida até `--d-stage-max`), com elementos principais em `position:absolute` cujo `top` é `%` (relativo à altura cheia do stage, nunca derivado de outro elemento) e `left`/`width` calculados por `gridX()` — um grid conceitual de 12 colunas com 24px de gap (`components/DesktopDobra.tsx`), sem exigir CSS Grid real. Tipografia de headline em `clamp()` baseado em `vw`. Dobra 9 (Impacto) adapta métricas para grid 2×2 (mobile: coluna única) como composição própria dessa faixa.
**Motivo:** Documentar formalmente uma decisão estrutural que já regia o desktop da Home havia várias rodadas (REGRA 5 do AGENT_RULES_SITE.md exige registro de toda decisão estrutural nova; esta ficou pendente).
**Impacto:** Nenhum — só documentação. Nenhum arquivo de código foi alterado por este registro.
**Status:** Confirmado (registro retroativo).

**Data:** 15/08/2026
**Decisão:** 28. Reconstrução da arquitetura de responsividade da Home (mobile + tablet), preservando a Decisão 27 (desktop) intocada. A prancheta de coordenadas por dobra da Decisão 26 (`top`/`left` em px, canvas fixo de 393px centralizado entre 394–1023px) deixa de ser a arquitetura de layout. 393×852 continua sendo a referência de calibração de cada lockup tipográfico (fonte/tracking/line-height) e de cada largura de bloco de texto, mas deixa de ser um viewport renderizado dentro de telas maiores. Mobile e tablet passam a ser um layout de FLUXO (flex column, `components/MobileDobra.tsx`: `MfFrame`/`MfLabel`/`MfTitle`/`MfBody`/`MfActions`/`MfButton`/`MfMetrics`), não mais coordenadas absolutas por elemento — cada dobra mantém sua própria combinação de blocos/gaps/alinhamento (não um grid genérico único, que já havia sido tentado e rejeitado na Decisão 25). Tablet (>=600px) ganha alinhamento editorial próprio (esquerda/direita, via atributo `data-tablet-align`, espelhando o `align` que a mesma dobra já usa no desktop) em vez de continuar centralizado como uma prancheta de celular esticada — é a diferença estrutural entre "tablet real" e "mobile escalado" pedida nesta tarefa. Altura da viewport passa a ser tratada explicitamente: espaçamento vertical entre blocos usa `rhythm()` (clamp em `svh`, `lib/responsive-type.ts`), que comprime em telas baixas (375×667, 360×640) e expande com moderação em telas altas, sempre ancorado na distância medida no frame 393×852 (não é escala proporcional da tela inteira — cada gap tem seu próprio floor/ceil). No desktop, font-size de headlines/nomes/corpo/métricas passou a usar `clamp()` com termo combinando `vw` E `svh` (não só `vw`), para não ficar apertado em notebooks baixos (1280×720, 1366×768) mesmo com largura sobrando.
**Motivo:** Pedido explícito do desenvolvedor: a Decisão 26 funcionava bem apenas em 393×852 (a viewport usada como referência de calibração) e tratava toda a faixa 394–1023px como uma prancheta de celular fixa centralizada — o tablet nunca existiu como experiência própria, e a altura da viewport não era considerada (393×667, 393×852 e 393×932 recebiam a mesma geometria vertical). Causa raiz identificada e confirmada antes da implementação: arquitetura de posicionamento absoluto por pixel copiada literalmente da referência de design, não uma arquitetura responsiva.
**Impacto:** `lib/mobile-fit.ts` foi removido (só escalava px entre 360–393px) e substituído por `lib/responsive-type.ts` (`lockup()` deriva font-size/line-height/letter-spacing fluidos e ancorados exatamente em 393px de largura a partir dos valores calibrados no Figma; `rhythm()`/`framePaddingTop()` geram espaçamento vertical fluido em `svh`; `contentWidth()` gera largura de bloco de texto fluida, também ancorada em 393px). `components/MobileDobra.tsx` foi reescrito (primitivos de fluxo em vez de `position:absolute`+`top`/`left` por instância). `components/HeroContent.tsx` e `components/HomeSections.tsx` tiveram o bloco mobile (`.mobile-fidelity`) reescrito dobra a dobra com os novos primitivos — nenhuma copy, cor, ordem narrativa ou line-break intencional foi alterada, exceto a correção de um gap de fidelidade pré-existente (não introduzido nesta rodada): a dobra de fechamento ("O QUE COMEÇOU...") nunca aplicava a cor de destaque em "OPERAÇÃO," (amarelo `#EDC812`) nem em "SUSTENTABILIDADE." (verde claro `#D3DEB4`) apesar de a especificação já pedir isso — corrigido em mobile E desktop, com um novo segmento `green` adicionado ao tipo `Segment` (`MobileDobra.tsx`/`DesktopDobra.tsx`), ao lado do `gold` já existente. `app/globals.css`: removida a media query que fixava `.mf-hero-frame`/`.mf-section-frame` em `width:393px` centralizado entre 394–1023px (a causa raiz); classes `.mf-d-*` deixaram de usar `position:absolute` para conteúdo principal (`.mf-bottom-fade`/`.mf-d-arrow`/`.mf-hero-scroll-label`, puramente decorativos, continuam absolutos, ancorados à base da viewport); `.d-name`/`.d-services`/`.d-body`/`.d-metric-value` (desktop) e `HEADLINE_FONT`/título do Hero/dobra 8 tiveram o termo `vw` do `clamp()` combinado com `svh`. Desktop (`.desktop-only`, motor de vídeo `transitionTo`/`playForward`/`interpolateBackward`, `lib/scroll-timeline.ts`, tempos do vídeo, `SCROLL_STOPS`) não foi tocado, exceto os `font-size` citados acima (mesmos valores mín/máx, só o termo fluido mudou de `vw` para `vw + svh`). Build (`npm run build`) validado. Validação automatizada via Playwright (overflow horizontal/vertical de todo conteúdo principal, span a span nos títulos) em 13 viewports (360×640 a 1920×1080) × 9 dobras: nenhum problema encontrado após os ajustes desta rodada. Validação visual por screenshot em 393×852 (fidelidade contra os 9 prints de referência), 768×1024 (tablet) e 1366×768 (desktop baixo).
**Status:** Confirmado. Esta decisão substitui o mecanismo de posicionamento da Decisão 26 para mobile/tablet; 393×852 continua sendo a referência de fidelidade máxima (Decisão 26 preservada nesse sentido), mas deixou de ser interpretada como arquitetura de layout fora dela.

## DECISÕES PENDENTES

- Confirmar número/link oficial do WhatsApp do Grupo Almeida para o botão principal da Home (atualmente apenas visual, sem destino configurado).
- Confirmar arquivo vetorial oficial da logo do Grupo Almeida (a Home está usando um placeholder textual "Grupo Almeida" em Playfair Display até o vetor chegar).
- Confirmar valor oficial do dourado/amarelo de destaque usado em "RESULTADO" na Home (atualmente token provisório `#D8A536` em `app/globals.css`).
- Avaliar engasgo real de seek em dispositivos de hardware mais limitado: o vídeo `Video_Almeida_15_seg.mp4` tem keyframes apenas em 0s/5s/10s (GOP de 5s); testes no Chromium desktop/mobile emulado não mostraram problema, mas vale reconfirmar em aparelho físico real antes do go-live.
